const initialState = {
  user: null,
  isAuthenticated: false,
};

const authSlice = (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authSlice;
